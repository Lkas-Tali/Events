<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme (night) -->
    <!-- By setting the parent to a Light theme, we are effectively disabling dark mode. -->
    <!-- The styles from the default values/themes.xml will be used. -->
    <style name="Theme.Events" parent="Base.Theme.Events" />
</resources>
