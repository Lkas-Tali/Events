<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme is already in themes.xml -->

    <!--
      FIX: This style has been updated to use a standard fade animation.
      The slide animation has been removed.
    -->
    <style name="FullScreenDialog" parent="Theme.MaterialComponents.Light.Dialog">
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowAnimationStyle">@style/DialogAnimation</item>
    </style>

    <!-- Dialog animation -->
    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

    <!--
      FIX: A new style has been added for the blurred dialog effect.
      It has a transparent background to allow the blur to show through.
    -->
    <style name="BlurredDialog" parent="Theme.MaterialComponents.Light.Dialog">
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@style/DialogAnimation</item>
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>
