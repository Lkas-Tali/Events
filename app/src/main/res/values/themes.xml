<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Events" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/app_primary_blue</item>
        <item name="colorPrimaryVariant">@color/app_primary_dark_blue</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/app_primary_blue</item>
        <item name="colorSecondaryVariant">@color/app_primary_dark_blue</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Background colors -->
        <item name="android:colorBackground">@color/app_background</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/app_text_primary</item>

        <!-- Error color -->
        <item name="colorError">@color/app_danger</item>
        <item name="colorOnError">@color/white</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Override Material button tint -->
        <item name="materialButtonStyle">@style/Widget.App.Button</item>
    </style>

    <style name="Theme.Events" parent="Base.Theme.Events" />

    <!-- Custom button style to ensure correct colors -->
    <style name="Widget.App.Button" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/app_primary_blue</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
