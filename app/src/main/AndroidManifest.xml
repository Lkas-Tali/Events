<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- This permission is required to connect to Firebase -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Permission for reading images from device storage -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- Permission for camera access (for profile image capture) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Camera feature declaration - not required because users can also select from gallery -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <!-- Camera2 feature declaration for newer camera API -->
    <uses-feature
        android:name="android.hardware.camera2"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Events"
        tools:targetApi="31">

        <!-- LoginActivity is now the LAUNCHER activity. -->
        <!-- This is the first screen the user will see. -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="stateVisible|adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity is now a regular activity with no special entry point. -->
        <!-- We will navigate to it from our LoginActivity code. -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Declaration for SignUpActivity to prevent app crash -->
        <activity
            android:name=".SignUpActivity"
            android:exported="false" />

        <!-- Declaration for CreateEventActivity -->
        <activity
            android:name=".CreateEventActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.Events" />

        <activity
            android:name=".ForgotPasswordActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

    </application>

</manifest>